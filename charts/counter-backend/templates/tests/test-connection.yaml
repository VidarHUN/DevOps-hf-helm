apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "counter-backend.fullname" . }}-test-connection"
  labels:
    {{- include "counter-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "counter-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
